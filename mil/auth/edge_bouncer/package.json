{
  "name": "mil-edge-bouncer",
  "version": "0.1.0",
  "private": true,
  "description": "MIL-61 Edge Bouncer — WorkOS JWT check at the Cloudflare edge",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251015.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4",
    "wrangler": "^3.80.0"
  }
}
