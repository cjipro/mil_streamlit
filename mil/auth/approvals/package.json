{
  "name": "mil-auth-approvals",
  "version": "0.1.0",
  "private": true,
  "description": "MIL-66a approved-user allowlist — gate edge-bouncer on per-email approval",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251015.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
