{
  "name": "mil-auth-audit",
  "version": "0.1.0",
  "private": true,
  "description": "MIL-65 immutable auth event audit log — shared lib used by edge-bouncer + magic-link Workers",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "verify": "node --experimental-strip-types src/verify_cli.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251015.0",
    "@types/node": "^25.6.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
