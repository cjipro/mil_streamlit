# Hypothesis declaration — shape preview, not yet validator-enforced.
# Loaded by the v1 synthesis layer (PULSE-93) to drive analytic + narrative
# rendering. Schema TBD; the fields here are illustrative of what the engine
# will need to know to produce a defensible decision pack output.

cell_id: 4
screen_id: international.beneficiary.setup
signature_id: dwell_after_error
ground_truth_expectation: positive

question_class: friction_attribution

analytic:
  method: dwell_z_score_vs_screen_baseline
  trigger:
    requires_prior_event: validation_error
    dwell_window_seconds: 90
    p_value_threshold: 0.01
  baseline_source: rolling_28d_same_screen
  minimum_sessions_for_significance: 200

cohort_axes:
  - first_time_international_sender
  - language_preference
  - corridor_risk_band
  - age_band

evidence_required:
  - dwell_time_seconds
  - error_event_count
  - error_type_distribution
  - cohort_split_breakdown
  - prior_field_focus_sequence

remediation_categories:
  - swift_lookup_affordance
  - sanctions_disclosure_clarity
  - latin_char_helper
  - intermediary_bank_auto_suggest

confidence:
  reporting: brier_calibrated
  band_method: bootstrap_ci_95

fairness:
  required_methods:
    - cohort_demographic_parity_check
    - cohort_recall_disparity
    - name_pattern_sanctions_disparity_check
  trigger_independent_review_if:
    cohort_recall_disparity_above: 0.15
    name_pattern_sanctions_disparity_above: 0.10

audit:
  bundle_required_fields:
    - lineage_chain
    - analytic_inputs_hash
    - cohort_split_breakdown
    - threshold_config_hash
    - sanctions_screening_decision_log_ref

# --- Canvas slots (PULSE-103: actors / value_inputs / risk_inputs) ---

actors:
  - investigation_consumer
  - compliance_reviewer

value_inputs:
  severity_class: high
  vulnerable_cohort_sensitivity: true
  population_segment_addressed: uk_retail_international_payment_initiators

risk_inputs:
  regulatory_taxonomies:
    - fca_consumer_duty.outcome_4_consumer_support
    - fca_operational_resilience.ps21_3
    - fatf.recommendation_10_customer_due_diligence
    - gdpr.article_32_security
  policy_areas:
    - international_payment_screening
    - sanctions_compliance
    - vulnerable_customer_handling
  chronicle_precedents:
    - CHR-friction-007
