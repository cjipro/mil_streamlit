"""Surface 2 — Investigation Workspace.

Three-altitude single-surface rendering of a decision pack. Filed under HOL-3.

v0 (this commit): renders pack metadata at three altitudes + lineage anchor +
Designed Ceiling notice (engine synthesis layer is interface-only; full
investigation rendering blocked on PULSE-93).
"""
